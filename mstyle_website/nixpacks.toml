[phases.setup]
nixPkgs = ["python311", "gcc", "freetype", "libpng", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn mstyle:app --bind 0.0.0.0:5000 --workers 2 --timeout 120"
